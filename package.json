{
  "name": "prime-collective-website",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server.js",
    "provision:gcp": "bash scripts/provision-gcp.sh",
    "deploy:vercel": "bash scripts/deploy.sh --target vercel --prod",
    "deploy:gcp": "bash scripts/deploy.sh --target gcp",
    "deploy:all": "bash scripts/deploy.sh --target all"
  },
  "engines": {
    "node": ">=18.17.0"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "sass": "^1.69.5",
    "sharp": "^0.34.5",
    "svgo": "^4.0.1",
    "terser": "^5.46.1",
    "vite": "^5.0.8"
  }
}
